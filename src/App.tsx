import React from 'react';
import Rotas from './rotas'; // Importando o arquivo de rotas

const App: React.FC = () => {
  return (
    <div>
      <Rotas /> {/* Carregando todas as rotas */}
    </div>
  );
};

export default App;
